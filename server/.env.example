ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Supabase project Settings → API → JWT Secret (legacy HS256 shared secret,
# used to verify the anonymous/linked-identity JWTs issued to the client)
SUPABASE_JWT_SECRET=your_supabase_jwt_secret

VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=your@email.com
PORT=3001
DB_PATH=./data/goals.db
